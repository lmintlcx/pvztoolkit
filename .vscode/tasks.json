{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "${workspaceRoot}/build.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}